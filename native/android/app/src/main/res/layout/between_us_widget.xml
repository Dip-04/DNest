<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:gravity="center_vertical"
    android:orientation="vertical"
    android:padding="18dp">

    <TextView
        android:id="@+id/widget_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Between Us"
        android:textColor="#523848"
        android:textSize="18sp"
        android:textStyle="bold" />

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="42dp"
        android:contentDescription="@string/route_between_partners"
        android:src="@drawable/route_line" />

    <TextView
        android:id="@+id/widget_people"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="Me  ♥  Partner"
        android:textColor="#725A68"
        android:textSize="13sp" />

    <TextView
        android:id="@+id/widget_distance"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:paddingTop="5dp"
        android:text="Waiting for both locations"
        android:textColor="#30242B"
        android:textSize="18sp"
        android:textStyle="bold" />
</LinearLayout>

