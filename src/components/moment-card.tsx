import Image from "next/image";import {CalendarDays,MapPin} from "lucide-react";import type {Moment} from "@/types/database";
export function MomentCard({moment,imageUrl}:{moment:Moment;imageUrl?:string}){return <article className="surface overflow-hidden rounded-[1.6rem]">{imageUrl?<div className="relative aspect-[4/3]"><Image src={imageUrl} fill sizes="(max-width: 768px) 100vw, 33vw" alt={`A memory from ${moment.title}`} className="object-cover"/></div>:<div className="grid aspect-[4/3] place-items-center bg-[linear-gradient(145deg,var(--rose-soft),var(--surface))] text-5xl" aria-hidden>♡</div>}<div className="p-5"><div className="flex items-center justify-between gap-2"><span className="chip">{moment.category}</span>{moment.mood&&<span className="muted text-xs">{moment.mood}</span>}</div><h2 className="display mt-5 text-3xl">{moment.title}</h2><p className="muted mt-2 line-clamp-3 text-sm leading-6">{moment.story||"A moment kept between the two of you."}</p><div className="muted mt-5 flex flex-wrap gap-3 text-xs"><span className="flex items-center gap-1"><CalendarDays className="size-3"/>{new Intl.DateTimeFormat("en",{dateStyle:"medium"}).format(new Date(moment.moment_at))}</span>{moment.location_name&&<span className="flex items-center gap-1"><MapPin className="size-3"/>{moment.location_name}</span>}</div></div></article>}
