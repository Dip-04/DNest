"use client";
import {useEffect,useState} from "react";import {countdown} from "@/lib/date";
export function MeetupCountdown({target}:{target:string}){const [value,setValue]=useState(()=>countdown(target));useEffect(()=>{const timer=setInterval(()=>setValue(countdown(target)),60_000);return()=>clearInterval(timer)},[target]);if(value.isPast)return <p className="display text-3xl">Today is the day ♥</p>;return <div aria-label={`${value.days} days, ${value.hours} hours, ${value.minutes} minutes until meetup`} className="grid grid-cols-3 gap-2">{[[value.days,"days"],[value.hours,"hours"],[value.minutes,"minutes"]].map(([n,label])=><div className="rounded-2xl bg-black/10 p-3 text-center dark:bg-white/10" key={label}><strong className="display block text-3xl">{n}</strong><span className="text-xs opacity-70">{label}</span></div>)}</div>}
