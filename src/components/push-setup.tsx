"use client";
import {useState} from "react";import {BellRing} from "lucide-react";
function decode(value:string){const padding="=".repeat((4-value.length%4)%4);const raw=atob((value+padding).replace(/-/g,"+").replace(/_/g,"/"));return Uint8Array.from([...raw].map(c=>c.charCodeAt(0)))}
export function PushSetup(){const [message,setMessage]=useState("");const key=process.env.NEXT_PUBLIC_VAPID_PUBLIC_KEY;async function enable(){if(!key||!("serviceWorker" in navigator)||!("PushManager" in window)){setMessage("Push is not configured for this deployment.");return}const permission=await Notification.requestPermission();if(permission!=="granted"){setMessage("Push permission was not granted. In-app notifications still work.");return}const registration=await navigator.serviceWorker.ready;const subscription=await registration.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:decode(key)});const response=await fetch("/api/push/subscribe",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(subscription)});setMessage(response.ok?"Push notifications are enabled on this device.":"This device could not be registered.")}return <div><button type="button" className="btn btn-secondary" onClick={enable}><BellRing className="size-4"/>Enable push on this device</button>{message&&<p role="status" className="muted mt-2 text-xs">{message}</p>}</div>}
