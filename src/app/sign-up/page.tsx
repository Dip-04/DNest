import type {Metadata} from "next";import Link from "next/link"; import { AuthCard } from "@/components/auth-card"; import { signUp } from "@/features/auth/actions";import {privateMetadata} from "@/lib/seo";
export const metadata:Metadata=privateMetadata("Create your account");
export default async function Page({searchParams}:{searchParams:Promise<{message?:string}>}){const {message}=await searchParams;return <AuthCard title="Make a place for your story" subtitle="Start your account now. You’ll invite exactly one partner next." action={signUp} submitLabel="Create my account" message={message} fields={[{name:"name",label:"Your name",type:"text",autoComplete:"name"},{name:"email",label:"Email",type:"email",autoComplete:"email"},{name:"password",label:"Password",type:"password",autoComplete:"new-password",placeholder:"At least 10 characters"}]} footer={<>Already have a Nest? <Link className="text-[var(--rose-deep)]" href="/sign-in">Sign in</Link></>}/>}
