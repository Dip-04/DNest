import Link from "next/link";import {ArrowLeft,Gift,LockKeyhole} from "lucide-react";import {notFound} from "next/navigation";import {createClient} from "@/lib/supabase/server";import {countdown} from "@/lib/date";
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const supabase=await createClient();const {data:meta}=await supabase.from("time_capsules").select("id,title,unlock_at,opened_at").eq("id",id).maybeSingle();if(!meta)notFound();const locked=new Date(meta.unlock_at)>new Date();let content:string|undefined;if(!locked){const {data}=await supabase.rpc("open_time_capsule",{p_capsule_id:id});content=data?.[0]?.content}const remaining=countdown(meta.unlock_at);return <div className="mx-auto max-w-2xl"><Link href="/us" className="muted flex items-center gap-2 text-sm"><ArrowLeft className="size-4"/>Back to Us</Link><section className="surface card mt-7 p-8 text-center"><div className="mx-auto grid size-16 place-items-center rounded-3xl bg-[var(--rose-soft)]">{locked?<LockKeyhole className="size-7 text-[var(--rose-deep)]"/>:<Gift className="size-7 text-[var(--rose-deep)]"/>}</div><span className="eyebrow mt-6 block">{locked?"Sealed for your future":"Ready for the two of you"}</span><h1 className="display mt-3 text-5xl">{meta.title}</h1>{locked?<><p className="muted mt-4">This message stays on the server until its moment arrives.</p><p className="display mt-7 text-3xl">{remaining.days} days · {remaining.hours} hours</p></>:<div className="mt-8 rounded-3xl bg-[var(--rose-soft)] p-7 text-left"><p className="display whitespace-pre-wrap text-2xl leading-9">{content}</p></div>}</section></div>}
